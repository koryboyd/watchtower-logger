# Installation

1. Ensure your bot runs Python 3.10+ and uses discord.py v2.x.
2. Copy repo files into your bot project (or use as a submodule).
3. Install dependencies:
   ```
   python -m pip install -r requirements.txt
   ```
4. Set environment variables (see docs/configuration.md).
5. Load the cog (see docs/integration.md).